import sys
import math

L = float(input("Input the length of number:"))
B = float(input("Input the width of number: "))
area_of_rect = L * B
perimeter_of_rec = 2 * (L + B)
print(area_of_rect)
print(perimeter_of_rec)



